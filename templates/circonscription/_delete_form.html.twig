<form method="post" action="{{ path('app_circonscription_delete', {'id': circonscription.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ circonscription.id) }}">
    <button class="btn">Delete</button>
</form>
